[project]
name = "ai-trip-planner1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "krupa", email = "krupajethwa123@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.6",
]

[project.scripts]
ai-trip-planner1 = "ai_trip_planner1:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
